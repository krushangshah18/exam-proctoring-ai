services:
  redis:
    image: redis:7-alpine
    container_name: exam_proctoring_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redisdata:/data

volumes:
  redisdata:
